class Solution {
public:
int gcd(int a,int b){
    if(a==0) return b;
    return gcd(b%a,a);
}
    int findGCD(vector<int>& arr) {
        sort(arr.begin(),arr.end());
        int x=arr[0];
        int y=arr[arr.size()-1];
         return gcd(x,y);
    }
};
